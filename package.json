{
  "name": "playwright-test-examples-ts",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript port of the C# PlaywrightTestExamples UI automation suite.",
  "scripts": {
    "install:browsers": "playwright install",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
