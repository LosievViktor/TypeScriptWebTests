# WebTests (TypeScript)
TypeScript code examples for Web UI Automation Tests made with Playwright library.

## How to launch

1. Install dependencies:

   ```
   npm install
   ```

2. Install Playwright browsers:

   ```
   npm run install:browsers
   ```

3. Run the tests:

   ```
   npm test
   ```
